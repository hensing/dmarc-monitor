services:
  dmarc-monitor:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./data:/app/data
    restart: unless-stopped
    command: ["-c", "/app/config.toml"]
